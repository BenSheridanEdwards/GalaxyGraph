{
  "name": "galaxy-graph-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm -w @galaxy-graph/core run build && npm -w @galaxy-graph/adapters run build && npm -w @galaxy-graph/cli run build && npm -w @galaxy-graph/example-basic run build",
    "typecheck": "npm -w @galaxy-graph/core run typecheck && npm -w @galaxy-graph/adapters run typecheck && npm -w @galaxy-graph/cli run typecheck && npm -w @galaxy-graph/example-basic run typecheck",
    "test": "vitest run",
    "dev": "npm -w @galaxy-graph/example-basic run dev"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
